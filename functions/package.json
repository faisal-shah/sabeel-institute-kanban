{
  "name": "functions",
  "version": "0.0.0",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && node esbuild.config.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --dir test/unit",
    "//test:integration": "Split in two, run against DIFFERENT emulator sets (see scripts/test-emulator.sh). RULES tests use clearFirestore, which fires triggers \u2014 running them with NO functions emulator means onCardDeleted never fires on the mass-deletes and cannot race a reused card id. FN tests genuinely exercise triggers/callables and need functions. New rules-style tests: add to :rules. New trigger/callable tests: add to :fn (they will fail fast in :rules without a functions runtime). Bounded --retry absorbs transient emulator hiccups; a real failure still fails all attempts. Unit tests stay strict.",
    "test:integration:rules": "vitest run test/integration/cardRules.test.ts test/integration/boardRules.test.ts test/integration/commentRules.test.ts test/integration/attachmentRules.test.ts test/integration/labelRules.test.ts test/integration/statsRules.test.ts test/integration/rules.test.ts test/integration/concurrentMoves.test.ts --no-file-parallelism --retry=2",
    "test:integration:fn": "vitest run test/integration/authTrigger.test.ts test/integration/setUserAccess.test.ts test/integration/cardTriggers.test.ts test/integration/attachments.test.ts test/integration/labels.test.ts test/integration/boardCardCount.test.ts test/integration/notifications.test.ts test/integration/notificationSweep.test.ts test/integration/stats.test.ts test/integration/health.test.ts --no-file-parallelism --retry=2",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "@sentry/node": "^10.66.0",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "esbuild": "^0.28.1",
    "firebase": "^12.15.0",
    "typescript": "~6.0.3",
    "vitest": "^4.1.10"
  }
}
