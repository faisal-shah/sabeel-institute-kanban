/**
 * Cloud Functions entry point.
 *
 * Phase 0: nothing deployed yet. Auth (domain enforcement + approval) lands in
 * Phase 1, triggers from Phase 8 onward. See docs/PHASE_STATUS.md.
 */

// Re-exported so the bundle has a stable shape from the start; real functions
// are appended here as their phases land.
export {};
